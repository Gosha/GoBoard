# TODO

- [x] Add a settings program accessible on both the desktop and the SteamVR dashboard, with controls for keyboard size and sound.
- [x] Don't completely disable the keyboard on unknown input language. Try to keep what we know as default
- [ ] Make sure keys are always sent, even when no textbox is selected. For instance, (double) pressing the windows key while the settings window is open, should open the windows menu.
- [ ] Spacing looks odd; spacing between keys is smaller than above/below. Let's make it consistent. Prefer smaller margins
- [ ] Add a SteamVR application manifest to enable autostart.
- [ ] Add reset button to keyboard that moves it back.
- [ ] Implement automatic keyboard layouts (see [research notes](docs/automatic-keyboard-layouts-research.md)).
